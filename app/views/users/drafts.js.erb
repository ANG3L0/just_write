$('#artview').append("<%= escape_javascript(render :partial => 'users/draft', :collection => @drafts) %>");
<% if @drafts.next_page %>
	$('.pagination').replaceWith('<%= j will_paginate @drafts %>')
<% else %>
	$(window).off('scroll');
	$('.pagination').remove();
<% end %>
